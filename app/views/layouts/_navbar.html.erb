<nav class="navbar navbar-expand-sm sticky-top navbar-dark bg-light">
  <div class="container-fluid">
    <div>
      <%= link_to 'Home', posts_path, class: 'btn btn-primary' %>
      <% if user_signed_in? %>
        <%= link_to 'My Posts', users_index_path, class: 'btn btn-secondary' %>
      <% end %>
      <% if user_signed_in? && 'admin' == current_user.genre%>
        <%= link_to 'Disasters Types', disasters_path, class: 'btn btn-dark' %>
      <% end %>
    </div>
    <% if user_signed_in? %>
      <h2> Hello <%= current_user.email %> </h2>
      <%= link_to 'Sign out', destroy_user_session_path, data: { 'turbo-method': :delete } %>
    <% else %>
      <%= link_to 'Sign in', new_user_session_path %>
    <% end %>
  </div>

</nav